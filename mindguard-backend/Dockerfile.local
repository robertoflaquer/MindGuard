FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY src/ ./src/
EXPOSE 3000
CMD ["sh", "-c", "node src/database/migrate.js && node src/database/migrate_appointments.js && node src/server.js"]
