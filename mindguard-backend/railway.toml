[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node src/database/migrate.js; node src/database/migrate_appointments.js; node src/server.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
